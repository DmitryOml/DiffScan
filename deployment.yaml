apiVersion: v1
kind: Namespace
metadata:
  name: diff
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: diff-checker
  namespace: diff
spec:
  replicas: 1
  selector:
    matchLabels:
      app: diff-checker
  template:
    metadata:
      labels:
        app: diff-checker
    spec:
      containers:
        - name: diff-checker
          image: 172.30.0.63:18443/diff-checker
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: diff-checker
  namespace: diff
spec:
  selector:
    app: diff-checker
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: diff-checker
  namespace: diff
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: diff.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: diff-checker
                port:
                  number: 80
